<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="probando.apli">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Datos_del_Ciudadano" />
        <activity android:name=".Direccion_ciudadano" />
        <activity android:name=".Vehiculo" />
        <activity android:name=".Leyes" />
        <activity android:name=".Remolque" />
        <activity android:name=".agregar_funcionario" />
        <activity android:name=".Funcionario" />
        <activity android:name=".Anadir_Leyes" />
        <activity android:name=".pdf"></activity>
    </application>

</manifest>